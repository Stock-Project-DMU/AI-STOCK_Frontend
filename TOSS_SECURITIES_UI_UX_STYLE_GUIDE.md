# AI-STOCK 토스증권형 UI/UX 스타일 가이드

> 조사일: 2026-09-16  
> 적용 대상: AI-STOCK 웹 프론트엔드(모바일 우선, 데스크톱 확장)  
> 문서 목적: 토스증권을 복제하는 것이 아니라, 복잡한 투자를 쉽게 이해하고 안전하게 실행하게 만드는 원칙을 AI-STOCK의 디자인 언어로 재해석한다.

## 1. 한 문장 정의

AI-STOCK의 UI는 **복잡한 투자 정보를 평범한 말과 분명한 위계로 보여주고, 사용자가 현재 상태와 다음 행동을 망설임 없이 이해하게 하는 차분한 금융 도구**여야 한다.

스타일 키워드는 다음 다섯 가지다.

- 쉬운 금융: 초보자가 배워야만 쓸 수 있는 화면을 만들지 않는다.
- 한 번에 하나: 한 화면과 한 영역에는 가장 중요한 목표 하나만 둔다.
- 숫자가 주인공: 장식보다 자산, 가격, 손익, 주문 상태가 먼저 보인다.
- 정직한 확신: 혜택뿐 아니라 수수료, 위험, 결과도 결정 전에 명확히 보여준다.
- 끊김 없는 흐름: 모바일과 PC, 탐색과 주문, 로딩과 완료가 같은 맥락으로 이어진다.

## 2. 조사 요약

토스증권은 공식적으로 투자의 어려움을 낮추는 방향을 강조하며, 모바일과 PC에서 이어지는 경험, 국내·해외주식의 일관된 사용 방식, 커뮤니티와 다양한 투자 상품을 주요 가치로 제시한다. 토스증권 UX R&D 역시 기존 증권 UX의 관습보다 복잡함 제거와 진입장벽 축소를 핵심 과제로 설명한다.

외부 사용자 조사에서도 토스증권은 주요 증권 앱 가운데 사용성·디자인·정보 전달력을 포함한 UX 전반에서 높은 평가를 받았다. 다만 이 결과는 모든 사용자가 동일하다는 뜻이 아니므로, AI-STOCK은 초보 투자자와 반복 거래 사용자의 밀도 요구를 구분해야 한다.

따라서 토스증권형 경험은 단순한 시각 모방이 아니다. 핵심은 다음 구조다.

1. 전문 지식을 요구하는 대신 필요한 정보를 그 순간에 설명한다.
2. 화면의 우선순위를 하나로 제한하고 CTA를 명확히 한다.
3. 금액과 결과를 행동 전에 미리 보여 심리적 불확실성을 줄인다.
4. 반복 사용자는 더 빠르게, 초보자는 더 안전하게 같은 흐름을 이용하게 한다.
5. 문구·상태·피드백까지 하나의 디자인 시스템으로 관리한다.

## 3. 제품 원칙

### 3.1 1초 안에 이해

사용자는 화면에 들어온 직후 아래 세 가지를 알 수 있어야 한다.

- 어디인가: 페이지 제목 또는 현재 종목
- 어떤 상태인가: 잔고, 가격, 주문·시장 상태
- 무엇을 할 수 있는가: 가장 중요한 CTA 하나

한 화면에 여러 행동이 필요하면 1차 행동만 채우고 나머지는 중립 또는 텍스트 버튼으로 낮춘다.

### 3.2 한 화면, 한 목표

- 종목 상세: 종목의 현재 상태를 파악한다.
- 주문 입력: 주문 조건을 만든다.
- 주문 확인: 비용과 결과를 검토한다.
- 주문 완료: 처리 결과와 다음 행동을 확인한다.

입력, 확인, 완료를 한 화면에 동시에 펼치지 않는다. 데스크톱에서 패널이 함께 보여도 시각적 초점은 현재 단계 하나에 둔다.

### 3.3 가치 먼저, 비용은 숨기지 않기

기능을 시작하기 전에 사용자가 얻을 가치를 알려준다. 다만 주문·결제·AI 추천에서는 수수료, 예상 체결 금액, 데이터 시점, 손실 가능성을 최종 행동 전에 반드시 노출한다.

### 3.4 점진적 공개

처음부터 모든 차트 지표와 주문 옵션을 보여주지 않는다.

- 기본: 현재가, 등락, 보유 수량, 평가손익, 기본 주문
- 확장: 호가, 거래량, 기간 수익률, 지정가 옵션
- 고급: 기술 지표, 체결 내역, 세부 재무 정보

고급 정보는 `더보기`, 탭, 펼침 영역으로 제공하되 사용자가 이미 본 기본 맥락을 잃지 않게 한다.

### 3.5 빠르되 안전하게

- 일반 탐색은 즉시 반응하고 낙관적 UI를 사용할 수 있다.
- 매수·매도·취소·충전처럼 금전 상태를 바꾸는 행동은 요약 확인과 서버 확정 결과를 구분한다.
- 치명적 행동은 색만으로 경고하지 않고 동사, 금액, 결과를 함께 쓴다.

## 4. 정보 구조

### 모바일

하단 내비게이션은 4~5개 핵심 목적만 유지한다.

1. 홈: 내 자산과 오늘의 핵심 변화
2. 발견: 검색, 시장, 종목 탐색
3. AI: 재무 설계, 시뮬레이션, 브리핑
4. 소식: 뉴스와 알림
5. 내 정보: 계좌, 설정, 문의

현재 위치는 색, 아이콘, 텍스트를 함께 사용해 표시한다.

### 데스크톱

- 좌측 또는 상단 전역 내비게이션: 목적지 이동
- 중앙 콘텐츠: 현재 과업
- 우측 보조 패널: 주문, 관심종목, AI 보조 정보
- 최대 콘텐츠 폭: 읽기 화면 `720~960px`, 대시보드 `1200~1440px`

우측 패널은 중앙 콘텐츠의 경쟁자가 아니라 현재 맥락을 보조해야 한다. 모바일에서는 바텀시트 또는 별도 단계로 전환한다.

## 5. 시각 스타일

기존 [`DESIGN-toss.md`](./DESIGN-toss.md)의 측정 토큰을 시각 기준으로 사용하고, 이 문서는 UX 적용 규칙을 정의한다.

### 5.1 색상

| 역할 | 토큰 | 값 | 용도 |
|---|---|---:|---|
| Primary | `--color-primary` | `#3182F6` | CTA, 링크, 선택, 포커스 |
| Primary active | `--color-primary-active` | `#2272EB` | hover/pressed |
| Primary tint | `--color-primary-tint` | `#EBF4FF` | 선택 배경, 정보 배너 |
| Ink | `--color-ink` | `#1C1F25` | 제목, 핵심 숫자 |
| Body | `--color-body` | `#4E535C` | 본문, 보조 숫자 |
| Muted | `--color-muted` | `#8F959E` | 캡션, 시점, 단위 |
| Hairline | `--color-hairline` | `#E8EBF0` | 구분선, 입력 경계 |
| Surface | `--color-surface-strong` | `#F4F5F8` | 보조 버튼, 입력 배경 |
| Canvas | `--color-canvas` | `#FFFFFF` | 주 화면 |
| 상승 | `--color-up` | `#CF202F` | 국내 증권 관례의 상승 |
| 하락 | `--color-down` | `#1D4ED8` | 국내 증권 관례의 하락 |
| 성공 | `--color-positive` | `#05B169` | 완료·정상 상태 |

토스 공식 브랜드 블루는 `#0064FF`지만, AI-STOCK은 공식 토스 브랜드를 사칭하지 않는다. 제품 인터랙션은 현재 코드의 `#3182F6`을 유지하고 토스 로고나 브랜드 자산을 사용하지 않는다.

색상 규칙:

- 파란색은 “누를 수 있음/선택됨”에 우선 사용한다.
- 상승·하락색은 값과 작은 지표에 사용하고 큰 카드 배경을 채우지 않는다.
- 본문은 순수 검정 대신 Ink/Body 계층을 사용한다.
- 상태 전달을 색 하나에 맡기지 않는다. 아이콘, 라벨, 기호를 함께 쓴다.

### 5.2 타이포그래피

기본 글꼴은 Pretendard다. 숫자는 같은 서체의 `tabular-nums`를 사용해 갱신 시 폭이 흔들리지 않게 한다.

| 역할 | 크기/행간 | 굵기 | 예시 |
|---|---|---:|---|
| 자산 숫자 | `32/38` | 700 | `12,450,000원` |
| 화면 제목 | `22/29` | 700 | `내 투자` |
| 섹션 제목 | `17/23` | 600 | `보유 주식` |
| 핵심 본문 | `15/23` | 500 | 종목명, 주요 설명 |
| 일반 본문 | `14/21` | 400~500 | 상세 설명 |
| 라벨 | `13/18` | 600 | 탭, 입력 라벨 |
| 캡션 | `12/17` | 500 | 업데이트 시각, 단위 |

금융 숫자 규칙:

- 금액: `12,450,000원`
- 가격: `74,200원`
- 수익률: `+3.42%`, `-1.08%`
- 손익: `+124,500원`
- 수량: `15주`
- 큰 숫자는 단위와 본체를 시각적으로 분리하되 읽는 순서는 유지한다.
- 표와 목록의 숫자는 우측 정렬한다.

### 5.3 형태와 깊이

- 버튼·입력: `12px`
- 작은 행·컨트롤: `8px`
- 카드·모달: `16px`
- 칩·아바타·토글만 완전한 pill
- 기본 화면은 평평하게 유지하고, 경계는 `1px` hairline 또는 표면색 차이로 만든다.
- 그림자는 메뉴, 팝오버, 모달처럼 실제로 떠 있는 요소에만 사용한다.

카드를 모든 콘텐츠의 기본 컨테이너로 사용하지 않는다. 같은 맥락의 목록은 여백과 구분선만으로 묶어 정보 밀도를 유지한다.

### 5.4 간격과 레이아웃

4px 기준 스케일: `4 / 8 / 12 / 16 / 20 / 24 / 32 / 48`.

- 모바일 좌우 안전 여백: `20px`
- 데스크톱 패널 내부: `20~24px`
- 목록 행 최소 높이: `56px`
- 일반 버튼 높이: `48px`
- 주요 CTA 높이: `52~56px`
- 섹션 간격: `32~48px`

밀도는 “여백을 많이 넣는 것”이 아니라 중요도가 다른 정보 사이에 간격 차이를 주는 방식으로 만든다.

## 6. 핵심 컴포넌트

### 버튼

- Primary: 화면의 최우선 행동, 화면당 보통 1개
- Secondary: 취소, 나중에, 보조 행동
- Tertiary: 상세 보기, 수정, 더보기
- Destructive: 삭제·강제취소 등 실제 파괴 행동

버튼 문구는 명사보다 결과가 예상되는 동사를 쓴다.

- 좋음: `10주 매수하기`, `주문 취소하기`, `목표 저장하기`
- 피함: `확인`, `다음`, `완료`, `예`

### 입력

- placeholder가 라벨을 대체하지 않는다.
- 숫자 입력에는 단위와 가능한 범위를 가까이 둔다.
- 입력 중 오류는 필드 아래에서 즉시 설명하고, 제출 후에만 한꺼번에 보여주지 않는다.
- 금액 입력에는 `전액`, `50%`, `10만원` 같은 비용 절감형 빠른 선택을 제공한다.

### 종목 행

정보 순서는 `로고/심볼 → 종목명·코드 → 현재가 → 등락`이다. 관심 여부는 별도 아이콘으로 제공하되 행 전체의 탐색 클릭을 방해하지 않는다.

### 카드

카드는 하나의 질문에 답해야 한다.

- “내 총자산은 얼마인가?”
- “오늘 얼마나 변했는가?”
- “이 주문은 어떤 상태인가?”

한 카드에 두 개 이상의 독립 CTA를 넣지 않는다.

### 탭과 필터

- 탭: 같은 위계의 콘텐츠 전환
- 세그먼트: 2~3개의 즉시 전환
- 칩: 복수 필터 또는 빠른 선택
- 드롭다운: 선택지가 많거나 빈도가 낮을 때

선택 상태는 글자 굵기와 색 또는 배경을 함께 바꾼다.

### 차트

- 기본적으로 선 하나와 현재 값에 집중한다.
- 축·격자·툴팁은 데이터 해석에 필요한 만큼만 남긴다.
- 기간 선택은 `1일 / 1주 / 1개월 / 1년 / 전체`처럼 예측 가능하게 유지한다.
- 실시간이 아니면 데이터 기준 시각을 반드시 표시한다.
- 상승·하락색만으로 추세를 설명하지 않는다.

## 7. 핵심 화면 패턴

### 7.1 홈

우선순위는 `총자산 → 오늘의 변화 → 보유 자산 → 필요한 알림 → 탐색`이다.

- 상단에 프로모션보다 내 자산을 둔다.
- 오늘 변화는 금액과 비율을 함께 제공한다.
- 빈 계좌에는 차트 대신 시작할 수 있는 한 가지 행동을 제안한다.
- 뉴스와 AI 인사이트는 개인 자산 맥락이 있을 때 우선 노출한다.

### 7.2 종목 상세

상단 첫 화면에는 종목명, 현재가, 등락, 시장 상태, 차트, 매수/매도 진입만 둔다. 재무, 뉴스, 커뮤니티, 기업 정보는 아래 또는 탭으로 단계화한다.

### 7.3 주문

`종목 확인 → 주문 방식 → 수량/금액 → 예상 결과 → 최종 확인 → 처리 결과` 순서를 유지한다.

최종 확인 화면에는 최소한 다음을 포함한다.

- 종목명과 매수/매도
- 수량, 주문 가격 또는 시장가
- 예상 주문 금액
- 수수료·세금 또는 산정 기준
- 주문이 즉시 체결되지 않을 가능성
- 실행 버튼에 구체적인 결과 동사

중복 주문 방지를 위해 제출 직후 버튼을 잠그고 처리 상태를 보여준다. 실패 시 사용자가 입력한 값은 보존한다.

### 7.4 AI 재무 설계·브리핑

- AI의 결론보다 근거, 데이터 기준 시점, 불확실성을 함께 보여준다.
- 추천과 주문 실행은 시각적·기능적으로 분리한다.
- AI 응답은 `요약 → 근거 → 주의할 점 → 사용자가 할 수 있는 다음 행동` 순으로 구성한다.
- 외부 API 실패를 “AI가 모른다”로 숨기지 말고 어떤 데이터가 갱신되지 않았는지 설명한다.
- 투자 권유처럼 단정하지 않고 시나리오와 선택지를 제공한다.

### 7.5 관리자

관리자 화면은 사용자용 화면보다 정보 밀도를 높여도 되지만 같은 토큰과 상태 문법을 유지한다.

- 검색·필터·목록·상세의 위치를 화면마다 바꾸지 않는다.
- 위험 행동은 사유 입력과 변경 결과 확인을 요구한다.
- 상태 변경 뒤에는 행과 상세 화면이 동시에 갱신되어야 한다.
- 감사 로그에 남는 행동임을 필요한 위치에서 알려준다.

## 8. UX 라이팅

보이스는 `명확한 / 간결한 / 친근한 / 존중하는 / 공감하는`으로 정한다.

| 피할 문구 | 권장 문구 |
|---|---|
| 주문 접수가 완료되었습니다 | 주문을 접수했어요 |
| 잔고 부족으로 처리 불가합니다 | 주문 가능 금액이 32,000원 부족해요 |
| 유효하지 않은 값입니다 | 1주 이상 입력해 주세요 |
| 확인하시겠습니까? | 삼성전자 10주를 742,000원에 주문할까요? |
| 데이터가 없습니다 | 아직 보유한 주식이 없어요 |
| 오류가 발생했습니다 | 시세를 불러오지 못했어요. 잠시 후 다시 시도해 주세요 |

문구 체크:

- 다음 화면이나 결과를 예상할 수 있는가?
- 금융 전문 용어를 일상어로 풀었는가?
- 없어도 뜻이 같은 단어와 문장을 제거했는가?
- 사용자를 겁주거나 행동을 강요하지 않는가?
- 혜택과 비용을 같은 기준으로 정직하게 썼는가?

## 9. 상태와 피드백

### 로딩

- 전체 화면 스피너보다 실제 레이아웃을 닮은 skeleton을 사용한다.
- 이전 데이터가 있으면 유지하고 갱신 중임을 작게 표시한다.
- 주문 처리 중에는 단계와 중복 실행 방지를 명확히 한다.

### 빈 상태

`현재 상태 → 이유 또는 맥락 → 다음 행동 하나` 구조를 쓴다.

예: “아직 관심 종목이 없어요. 자주 확인할 종목을 모아보세요. [종목 찾기]”

### 오류

- 무엇이 실패했는지
- 사용자가 입력한 내용이 보존됐는지
- 지금 할 수 있는 행동이 무엇인지

를 함께 알려준다. 오류 코드는 고객센터용 세부 정보로 접어 제공한다.

### 완료

완료 애니메이션보다 결과 요약을 우선한다. 주문은 `접수`, `체결`, `취소`를 같은 “완료”로 표현하지 않는다.

## 10. 모션

- 일반 전환: `160~220ms`
- 바텀시트·모달: `220~300ms`
- easing: 빠르게 반응하고 부드럽게 멈추는 `ease-out`
- hover에서 크기 변화는 최대 `1~2px` 수준으로 제한한다.
- 가격 갱신은 짧은 색 강조 후 원래 상태로 돌아오게 한다.
- `prefers-reduced-motion`에서는 이동과 반복 애니메이션을 제거한다.

모션은 결과를 설명하거나 공간 관계를 유지할 때만 쓴다. 투자 화면에서 장식 목적의 지속 애니메이션은 피한다.

## 11. 접근성

- 일반 텍스트 대비 `4.5:1` 이상, 큰 텍스트 `3:1` 이상
- 모든 인터랙션의 키보드 접근과 보이는 focus ring 제공
- 터치 영역 최소 `44×44px`
- 차트에 텍스트 요약 또는 데이터 표 대안 제공
- 상승/하락, 성공/실패를 색상 외 기호와 문구로 구분
- 모달은 포커스를 가두고 닫은 뒤 원래 위치로 복귀
- 실시간 가격 갱신은 스크린리더를 과도하게 방해하지 않도록 일괄 또는 요청 시 알림

## 12. 반응형 규칙

| 구간 | 원칙 |
|---|---|
| `< 768px` | 단일 열, 하단 내비게이션, 주문은 전체 화면/바텀시트 |
| `768~1199px` | 1~2열, 보조 정보는 접기 가능 |
| `≥ 1200px` | 주 콘텐츠 + 보조 패널, 주문 패널 고정 가능 |

반응형은 화면을 축소하는 작업이 아니다. 모바일에서는 부가 정보를 뒤로 보내고, 데스크톱에서는 같은 맥락을 병렬로 보여준다. 기능의 이름과 순서는 기기마다 바꾸지 않는다.

## 13. 금지 패턴

- 모든 버튼과 카드를 pill 또는 과도한 라운드로 만들기
- 큰 그림자, 유리 효과, 네온 glow를 기본 표면에 사용하기
- 파란색을 장식으로 남발해 클릭 가능 여부를 흐리기
- 한 화면에 동일 우선순위 CTA 여러 개 배치하기
- 수익만 크게 보여주고 손실 가능성·비용·데이터 시점을 숨기기
- 로그인하지 않은 사용자에게 로그인 이후 맥락을 설명 없이 요구하기
- 로딩 때문에 핵심 숫자가 계속 흔들리는 레이아웃 만들기
- `확인`, `다음`, `처리`처럼 결과가 모호한 버튼 문구 사용하기
- 토스 로고, 상표, 고유 그래픽을 복제해 공식 서비스처럼 보이게 만들기

## 14. 구현 우선순위

### P0 — 일관성

- 전역 색상·타이포·간격·radius 토큰 통일
- 공통 Button, Input, Modal, Tabs, Toast 상태 통일
- 숫자 포맷과 상승/하락 의미 통일
- 로딩·빈 상태·오류 상태 기본 컴포넌트 구축

### P1 — 핵심 투자 흐름

- 홈의 자산 우선순위 재정렬
- 종목 상세의 점진적 공개
- 주문 입력/확인/결과 단계 분리
- 실시간·지연 데이터 시점 표시

### P2 — AI 경험

- AI 답변 템플릿과 출처·기준 시점 표시
- 추천과 주문 실행 분리
- 실패·부분 데이터 상태 설계

### P3 — 검증

- 초보 투자자와 반복 거래 사용자로 과업 테스트
- 모바일 360px, 태블릿, 1440px 데스크톱 검수
- 키보드, 스크린리더, reduced motion 검수
- 첫 매수, 주문 취소, API 실패 시나리오 검증

## 15. PR 체크리스트

- [ ] 화면의 최우선 목표를 한 문장으로 말할 수 있다.
- [ ] 가장 중요한 CTA가 하나이며 결과를 동사로 설명한다.
- [ ] 사용자가 다음 단계와 비용·위험을 예측할 수 있다.
- [ ] 로딩, 빈 상태, 오류, 성공 상태가 모두 정의되어 있다.
- [ ] 금액·수량·수익률의 포맷과 정렬이 일관적이다.
- [ ] 상승/하락과 오류를 색상만으로 전달하지 않는다.
- [ ] 360px 모바일과 1440px 데스크톱에서 핵심 과업이 유지된다.
- [ ] 키보드와 focus 상태로 전체 흐름을 완료할 수 있다.
- [ ] 전문 용어와 중복 문장을 제거했다.
- [ ] 사용자를 조급하게 하거나 오해시키는 문구가 없다.

## 16. 참고 자료

- [토스증권 공식 홈페이지](https://home.tossinvest.com/ko) — 제품 미션, 투자 상품, 모바일·PC 연속 경험
- [토스증권 UX의 미래](https://toss.im/tmc-25/sessions/design/ui-design-20) — 복잡함 제거와 투자 진입장벽 축소
- [증권 UX 리서처가 투자의 비결을 알게 되기까지](https://toss.tech/article/uxresearcher-meets-investor) — 초보 투자자의 첫 매수와 관심종목 문제
- [크고 복잡한 제품, 과감하게 갈아엎기](https://toss.tech/article/mydoc) — Simplicity, One thing per One page, Clear CTA, Sleek Experience
- [토스의 8가지 라이팅 원칙](https://toss.tech/article/8-writing-principles-of-toss) — 명확성, 간결성, 예측 가능한 힌트, 쉬운 언어
- [마케팅 문구 클릭률을 올리는 6가지 원칙](https://toss.tech/article/Marketing_Writing) — 과장과 불안 자극을 피하고 한 메시지에 집중하는 최신 라이팅 사례
- [토스 브랜드 리소스 센터](https://brand.toss.im/) — 공식 브랜드 색상과 자산 사용 규정
- [오픈서베이 주요 증권 앱 UX 비교](https://blog.opensurvey.co.kr/article/ux-finance-app-3/) — 사용자 평가 기반 UX 강점 및 세그먼트 고려사항

---

이 문서는 공개 자료를 바탕으로 정리한 AI-STOCK 내부 가이드다. 토스증권의 비공개 디자인 시스템을 대변하지 않으며, 토스 브랜드·화면을 그대로 복제하기 위한 문서가 아니다.
